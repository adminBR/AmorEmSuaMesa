<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amoremsuamesa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmorEmSuaMesa">
        <activity android:name=".UsuarioActivity"></activity>
        <activity
            android:name=".favoritos"
            android:theme="@style/Theme.AmorEmSuaMesa.NoActionBar" />
        <activity
            android:name=".teste"
            android:theme="@style/Theme.AmorEmSuaMesa.NoActionBar" />
        <activity
            android:name=".Carrinho"
            android:label="Seu carrinho de compras" />
        <activity
            android:name=".MainActivity"
            android:label="Amor em sua mesa"
            android:theme="@style/Theme.AmorEmSuaMesa.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>